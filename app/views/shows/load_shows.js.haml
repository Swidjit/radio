
$('#show-list').html('<h4>Shows</h4>#{j render @shows}');
$('#band-label').html("<div class='band-label show-bands'>#{@band.name}</div>");
$('#years').append('#{j render :partial => "bands/years"}');
